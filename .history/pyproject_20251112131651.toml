[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pdf-extract"
version = "0.1.0"
description = "PDF → NuExtract (OpenAI-compatible) JSON extraction pipeline"
readme = "README.md"
authors = [{name = "you"}]
requires-python = ">=3.10"
dependencies = [
  "requests>=2.31",
  "pypdf>=4.2.0",
  "python-dotenv>=1.0.1",
  "tomli>=2.0.1; python_version<'3.11'",
]

[project.scripts]
pdf-extract = "src.main:cli"
